# Scopus API Configuration
# Get your API key from: https://dev.elsevier.com/
SCOPUS_API_KEY=your_scopus_api_key_here

# Database (optional)
# If set to a PostgreSQL URL (e.g. Railway), app uses PostgreSQL; otherwise SQLite.
# DATABASE_URL=postgresql://user:password@host:port/database

# Flask Configuration
# Set to 'production' for production deployment
FLASK_ENV=production

# Server Configuration (optional; Railway sets PORT automatically)
# HOST=0.0.0.0
# PORT=5000
